# Kubernetes deployment management google function

This google function enables to administer the Kubernetes container deployments.

- create deployment
- update deployment replication ( scale action )
